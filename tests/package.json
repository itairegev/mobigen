{
  "name": "@mobigen/tests",
  "version": "0.1.0",
  "private": true,
  "description": "Test suite for Mobigen platform",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:api": "vitest run api/",
    "test:services": "vitest run services/",
    "test:e2e": "vitest run e2e/",
    "test:ui": "vitest run ui/",
    "test:all": "vitest run"
  },
  "devDependencies": {
    "@prisma/client": "^5.22.0",
    "@types/node": "^20.10.0",
    "socket.io-client": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
