# Analytics Infrastructure Environment Variables

# ClickHouse
CLICKHOUSE_PASSWORD=changeme123
CLICKHOUSE_USER=mobigen
CLICKHOUSE_DATABASE=mobigen_analytics

# TimescaleDB
TIMESCALE_PASSWORD=changeme123
TIMESCALE_USER=mobigen
TIMESCALE_DATABASE=mobigen_analytics

# Grafana
GRAFANA_PASSWORD=admin123

# For production, use strong passwords:
# CLICKHOUSE_PASSWORD=$(openssl rand -base64 32)
# TIMESCALE_PASSWORD=$(openssl rand -base64 32)
# GRAFANA_PASSWORD=$(openssl rand -base64 32)
