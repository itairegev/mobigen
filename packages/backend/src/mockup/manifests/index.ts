/**
 * Template Mockup Manifests
 *
 * Pre-defined screen layouts and branding for each template
 */

export { ecommerceManifest } from './ecommerce.manifest';
export { loyaltyManifest } from './loyalty.manifest';

// Re-export manifest type from UI package
export type { MockupManifest } from '@mobigen/ui/mockup';
